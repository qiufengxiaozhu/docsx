spring:
  datasource:
    url: jdbc:sqlite:${DOCSX_DB_PATH:./data/docsx.db}
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-sqlite.sql
