spring:
  datasource:
    url: jdbc:sqlite:${DOCSX_DATA_DIR:./data}/docsx.db
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-sqlite.sql
      continue-on-error: true
